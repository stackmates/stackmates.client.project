'use strict';

module.exports = angular.module('sm.viz.bar.progress', [])

// .controller('ScatterController', require('./controllers/scatter_plot'))

.directive('barProgress', require('./directives/bar_progress'))

;
